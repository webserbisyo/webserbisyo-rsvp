export default function PublicLandingPage() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-5xl flex-col justify-center gap-4 px-6 py-16">
      <p className="text-muted-foreground text-sm font-medium tracking-[0.2em] uppercase">
        Foundation Placeholder
      </p>
      <h1 className="text-4xl font-semibold tracking-tight">WebSerbisyo RSVP</h1>
      <p className="text-muted-foreground max-w-2xl text-base">
        Public landing page scaffold. Application features and content flows are intentionally not
        implemented in this setup commit.
      </p>
    </main>
  );
}
